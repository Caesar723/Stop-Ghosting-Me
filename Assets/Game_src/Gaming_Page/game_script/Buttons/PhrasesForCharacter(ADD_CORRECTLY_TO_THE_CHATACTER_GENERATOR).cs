using UnityEngine;

public class PhrasesForCharacter_ADD_CORRECTLY_TO_THE_CHATACTER_GENERATOR_ : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
